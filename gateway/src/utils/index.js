const apiFetch = require("./apiFetch");
const apiEnd = require("./apiEnd");
const sendJsonResponse = require("./sendJsonResponse");
const validateRegData = require("./validateRegData");

module.exports = {
  apiFetch,
  apiEnd,
  sendJsonResponse,
  validateRegData,
};
